void comandoSelecto()
{


  if (str == "el")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }


  if (str == "al")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "tad")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "tat")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "maa")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "maat")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mba")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mbat")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mca")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mcat")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mda")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
  if (str == "mdat")
  {
    Serial.println(str);
    conversionEnvio();
    limpiar();
  }
}
